import { describe, test } from 'vitest'

describe('App.ts', function () {
  test('displays a todo', async () => {})

  test('adds a new todo when clicking the button', async () => {})

  test('test error', async () => {})
})
